package br.com.ricardo.eloCRUD.domain;

import org.junit.jupiter.api.Test;

import java.time.LocalDate;

import static org.junit.jupiter.api.Assertions.*;
import static org.assertj.core.api.Assertions.assertThat;


/*class ComentarioTest {

    @Test
    public void criacaoDeComentarioTest() {
        Comentario comentario = new Comentario();

        comentario.setId(1l);
        comentario.setDescricao("Ótima tarefa!");
        comentario.setPessoaId(new Pessoa());
        comentario.setDataComentario(LocalDate.now());
        comentario.setTarefaNumero(new Tarefa());

        assertThat(comentario.getId()).isEqualTo(1L);
        assertThat(comentario.getDescricao()).isEqualTo("Ótima tarefa!");
        assertThat(comentario.getPessoaId()).isEqualTo(new Pessoa());
        assertThat(comentario.getDataComentario()).isEqualTo(LocalDate.now());
        assertThat(comentario.getTarefaNumero()).isEqualTo(new Tarefa());
    }

}*/