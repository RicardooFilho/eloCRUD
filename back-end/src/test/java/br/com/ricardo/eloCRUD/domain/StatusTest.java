package br.com.ricardo.eloCRUD.domain;

import br.com.ricardo.eloCRUD.enums.SituacaoEnum;
import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;


/*class StatusTest {

    @Test
    public void criacaoDeStatusTest() {
        Status status = new Status();

        status.setId(1L);
        status.setDescricao("Na fila");
        status.setSituacao(SituacaoEnum.PENDENTE);

        assertThat(status.getId()).isEqualTo(1L);
        assertThat(status.getDescricao()).isEqualTo("Na fila");
        assertThat(status.getSituacao()).isEqualTo(SituacaoEnum.PENDENTE);
    }

}*/